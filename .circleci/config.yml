version: 2
jobs:
  deploy:
    docker:
      - image: jumanjiman/aws:latest
    working_directory: ~/yukpiz
    steps:
      - checkout
      - run: apk add git
      - run: git submodule update --init --recursive
      - run: echo "(」・ω・)」うー！"
      - run: aws s3 cp ./slide/outputs/ s3://yukpiz/ --exclude "*" --include "*.html"
      - run: aws s3 cp ./slide/reveal.js s3://yukpiz/ --recursive
      - run: echo "(／・ω・)／にゃー！"

workflows:
  version: 2
  deploy:
    jobs:
      - deploy