version: 2
jobs:
  build:
    docker:
      - image: jumanjiman/aws:latest
    working_directory: ~/yukpiz
    steps:
      - run echo "hoge"
        #pwd
        #"type aws
        #  cat /etc/lsb-release
        #  echo "(／・ω・)／にゃー！"